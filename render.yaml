services:
  - type: web
    name: intern-vanna
    env: docker
    dockerfilePath: apps/vanna/Dockerfile
    plan: starter
    repo: <your-github-repo-url>
    branch: main
    buildCommand: ""
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
databases:
  - name: intern-db
    plan: starter
